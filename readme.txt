1. successfully submit with simplied data preprocessing, with only DepartmentDescription feature expansion.

Later add UPC and FinelineNumber (mostly accessed) feature expansion to have thousands of columns for prediction.

2. write functions to transform and aggregate DepartmentDescription with numbers for each VisitNumber-TripType.

(VisitNumber-TripType)  Mapping  (Department and numbers related).  Latter with more UPC and FinelineNumber expansion

3. xboosting just start, need to figure out how to have multi-varable predication and cross validation
